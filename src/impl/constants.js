////////////////////////////////////////////////////////////////////////////////
//
//    Copyright (c) 2022 - 2023.
//    Haixing Hu, Qubit Co. Ltd.
//
//    All rights reserved.
//
////////////////////////////////////////////////////////////////////////////////
/**
 * 被 `@Type` 修饰的字段的元信息属性名。
 *
 * @private
 */
const PROPERTY_TYPE = 'type';

/**
 * 被 `@ElementType` 修饰的字段的元信息属性名。
 *
 * @private
 */
const PROPERTY_ELEMENT_TYPE = 'element_type';

/**
 * 被 `@Normalizer` 修饰的字段的元信息属性名。
 *
 * @private
 */
const PROPERTY_NORMALIZER = 'normalizer';

/**
 * 被 `@Validator` 修饰的字段的元信息属性名。
 *
 * @private
 */
const PROPERTY_VALIDATOR = 'validator';

/**
 * 被 `@Nullable` 修饰的字段的元信息属性名。
 *
 * @private
 */
const PROPERTY_NULLABLE = 'nullable';

/**
 * 被 `@DisplayName` 修饰的字段的元信息属性名。
 *
 * @private
 */
const PROPERTY_DISPLAY_NAME = 'display_name';

/**
 * 被 `@NameField` 修饰的字段的元信息属性名。
 *
 * @private
 */
const PROPERTY_NAME_FIELD = 'name_field';

const PROPERTY_METADATA = '__metadata__';

const PROPERTY_DEFAULT_INSTANCE = 'default_instance';

const PROPERTY_FIELDS = 'fields';

export {
  PROPERTY_TYPE,
  PROPERTY_ELEMENT_TYPE,
  PROPERTY_NORMALIZER,
  PROPERTY_VALIDATOR,
  PROPERTY_NULLABLE,
  PROPERTY_DISPLAY_NAME,
  PROPERTY_NAME_FIELD,
  PROPERTY_METADATA,
  PROPERTY_DEFAULT_INSTANCE,
  PROPERTY_FIELDS,
};
